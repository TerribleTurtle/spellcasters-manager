# Data Directory — the path the server reads/writes all JSON and assets from.
# Change this path and restart to point at a different data set.
#
# Examples:
#   Live API data:  DATA_DIR=../spellcasters-community-api/data
#   Local sandbox:  DATA_DIR=./mock_data
DATA_DIR=../spellcasters-community-api/data

# Server port (default: 3001)
# PORT=3001

# Sync source — where `npm run sync-data` reads from (default: ../spellcasters-community-api/data)
# SYNC_SOURCE=../spellcasters-community-api/data
